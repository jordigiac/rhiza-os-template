# setup/ — exists only before onboarding

This folder holds the onboarding interview. It runs once, live on a call with
Jordi. When onboarding completes, the interview's answers are written into the
OS and this whole folder is deleted — its continued existence is how the OS
knows setup hasn't happened yet.
