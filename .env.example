# API keys live in a file named .env in this folder — this file is the example.
# Your OS sets this up with you when you connect a tool; you never edit it alone.
# Never put a key in any other file, and never paste one into chat.
# Each connected tool also gets a plain-English record in connections/.
#
# EXAMPLE_TOOL_API_KEY=
